FastCurrency - серверная часть приложения для конвертера валют, написанная на FastAPI.

Для запуска:

1)docker build -t <build:name> .

2)docker run -d -p 8000:8000 <build:name>

Документация:
Находится по адресу /docs

